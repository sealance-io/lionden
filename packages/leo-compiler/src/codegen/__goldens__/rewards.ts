// Auto-generated by @lionden/leo-compiler — do not edit
// Program: rewards.aleo

import { BaseContract, type LocalCallOptions, type BroadcastOptions, type TransitionCallResult } from "./BaseContract.js";

// ---------------------------------------------------------------------------
// Rewards contract wrapper
// ---------------------------------------------------------------------------

export class Rewards extends BaseContract {
  constructor() {
    super("rewards.aleo");
  }

  async earn_points(
    amount: bigint,
    options?: LocalCallOptions,
  ): Promise<void> {
    const _args: string[] = [
      amount.toString() + "u64",
    ];
    const _result = await this.executeLocal("earn_points", _args, options ?? {});
    return undefined as any;
  }
  
  async earn_pointsBroadcast(
    amount: bigint,
    options?: BroadcastOptions,
  ): Promise<TransitionCallResult> {
    const _args: string[] = [
      amount.toString() + "u64",
    ];
    return this.broadcast("earn_points", _args, options ?? {});
  }

  async claim_reward(
    reward_amount: bigint,
    options?: LocalCallOptions,
  ): Promise<void> {
    const _args: string[] = [
      reward_amount.toString() + "u64",
    ];
    const _result = await this.executeLocal("claim_reward", _args, options ?? {});
    return undefined as any;
  }
  
  async claim_rewardBroadcast(
    reward_amount: bigint,
    options?: BroadcastOptions,
  ): Promise<TransitionCallResult> {
    const _args: string[] = [
      reward_amount.toString() + "u64",
    ];
    return this.broadcast("claim_reward", _args, options ?? {});
  }

  async getPoints(key: string): Promise<bigint | null> {
    const _result = await this.queryMapping("points", key);
    if (_result === null) return null;
    return BaseContract.parseBigInt(_result);
  }

  async getClaimed(key: string): Promise<boolean | null> {
    const _result = await this.queryMapping("claimed", key);
    if (_result === null) return null;
    return BaseContract.parseBoolean(_result);
  }
}

export function createRewards(): Rewards {
  return new Rewards();
}
