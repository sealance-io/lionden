// Auto-generated by @lionden/leo-compiler — do not edit
// Program: token.aleo

import { BaseContract, type LocalCallOptions, type BroadcastOptions, type TransitionCallResult } from "./BaseContract.js";

export interface Token {
  readonly owner: string;
  readonly amount: bigint;
  readonly _nonce: string;
}

// ---------------------------------------------------------------------------
// Serialization / deserialization helpers
// ---------------------------------------------------------------------------

export function serializeToken(value: Token): string {
  const fields: string[] = [];
  fields.push("owner: " + value.owner);
  fields.push("amount: " + value.amount.toString() + "u64");
  fields.push("_nonce: " + value._nonce);
  return "{ " + fields.join(", ") + " }";
}

export function deserializeToken(value: string): Token {
  const _fields = BaseContract.parseStruct(value);
  return {
    owner: BaseContract.parseString(_fields["owner"]!),
    amount: BaseContract.parseBigInt(_fields["amount"]!),
    _nonce: BaseContract.parseString(_fields["_nonce"] ?? ""),
  };
}

// ---------------------------------------------------------------------------
// Token contract wrapper
// ---------------------------------------------------------------------------

export class Token extends BaseContract {
  constructor() {
    super("token.aleo");
  }

  async mint(
    receiver: string,
    amount: bigint,
    options?: LocalCallOptions,
  ): Promise<Token> {
    const _args: string[] = [
      receiver,
      amount.toString() + "u64",
    ];
    const _result = await this.executeLocal("mint", _args, options ?? {});
    return deserializeToken(_result.outputs[0]!);
  }
  
  async mintBroadcast(
    receiver: string,
    amount: bigint,
    options?: BroadcastOptions,
  ): Promise<TransitionCallResult> {
    const _args: string[] = [
      receiver,
      amount.toString() + "u64",
    ];
    return this.broadcast("mint", _args, options ?? {});
  }

  async mint_and_finalize(
    receiver: string,
    options?: LocalCallOptions,
  ): Promise<[Token, void]> {
    const _args: string[] = [
      receiver,
    ];
    const _result = await this.executeLocal("mint_and_finalize", _args, options ?? {});
    const _decoded: [Token, void] = [deserializeToken(_result.outputs[0]!), undefined as any];
    return _decoded;
  }
  
  async mint_and_finalizeBroadcast(
    receiver: string,
    options?: BroadcastOptions,
  ): Promise<TransitionCallResult> {
    const _args: string[] = [
      receiver,
    ];
    return this.broadcast("mint_and_finalize", _args, options ?? {});
  }

  async burn(
    token: Token,
    options?: LocalCallOptions,
  ): Promise<void> {
    const _args: string[] = [
      serializeToken(token),
    ];
    const _result = await this.executeLocal("burn", _args, options ?? {});
    return undefined as any;
  }
  
  async burnBroadcast(
    token: Token,
    options?: BroadcastOptions,
  ): Promise<TransitionCallResult> {
    const _args: string[] = [
      serializeToken(token),
    ];
    return this.broadcast("burn", _args, options ?? {});
  }

  async split(
    token: Token,
    options?: LocalCallOptions,
  ): Promise<[Token, Token]> {
    const _args: string[] = [
      serializeToken(token),
    ];
    const _result = await this.executeLocal("split", _args, options ?? {});
    const _decoded: [Token, Token] = [deserializeToken(_result.outputs[0]!), deserializeToken(_result.outputs[1]!)];
    return _decoded;
  }
  
  async splitBroadcast(
    token: Token,
    options?: BroadcastOptions,
  ): Promise<TransitionCallResult> {
    const _args: string[] = [
      serializeToken(token),
    ];
    return this.broadcast("split", _args, options ?? {});
  }

  async getSupply(key: string): Promise<bigint | null> {
    const _result = await this.queryMapping("supply", key);
    if (_result === null) return null;
    return BaseContract.parseBigInt(_result);
  }
}

export function createToken(): Token {
  return new Token();
}
