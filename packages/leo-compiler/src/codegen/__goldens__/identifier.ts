// Auto-generated by @lionden/leo-compiler — do not edit
// Program: governance.aleo

import { BaseContract, type AddressInput, type DynamicRecordInput, type FieldInput, type GroupInput, type IdentifierInput, type LeoAddress, type LeoDynamicRecord, type LeoField, type LeoGroup, type LeoIdentifier, type LeoPlaintext, type LeoScalar, type LocalExecutionOptions, type LocalTransitionError, type OnChainExecutionOptions, type PlaintextInput, type RecordDecryptionKey, type ScalarInput, type SettledTransition, type SubmittedTransition, type TransitionInputContext } from "./BaseContract.js";

export interface StrategyConfig {
  readonly strategy: LeoIdentifier;
  readonly fallback: LeoIdentifier | null;
  readonly strategies: ReadonlyArray<LeoIdentifier>;
}

export interface Vote {
  readonly owner: LeoAddress;
  readonly strategy: LeoIdentifier;
  readonly _nonce: LeoGroup;
}

// ---------------------------------------------------------------------------
// Serialization / deserialization helpers
// ---------------------------------------------------------------------------

export function serializeStrategyConfig(value: StrategyConfig, context?: TransitionInputContext): string {
  BaseContract.assertObject(value, context);
  const fields: string[] = [];
  fields.push("strategy: " + BaseContract.serializeIdentifier(value.strategy, BaseContract.childInputContext(context, "strategy")));
  fields.push("fallback: " + (value.fallback != null ? "{ is_some: true, val: " + BaseContract.serializeIdentifier(value.fallback, BaseContract.childInputContext(context, "fallback")) + " }" : "{ is_some: false, val: 'lionden_zero' }"));
  fields.push("strategies: " + BaseContract.serializeArray(value.strategies, BaseContract.childInputContext(context, "strategies"), (e: unknown, context: TransitionInputContext | undefined) => BaseContract.serializeIdentifier(e, context)));
  return "{ " + fields.join(", ") + " }";
}

export function deserializeStrategyConfig(value: string): StrategyConfig {
  const _fields = BaseContract.parseStruct(value);
  return {
    strategy: BaseContract.parseIdentifier(_fields["strategy"]!),
    fallback: (() => { const _opt = BaseContract.parseStruct(_fields["fallback"]!); return BaseContract.parseBoolean(_opt["is_some"]!) ? BaseContract.parseIdentifier(_opt["val"]!) : null; })(),
    strategies: BaseContract.parseArray(_fields["strategies"]!).map((e: string) => BaseContract.parseIdentifier(e)),
  };
}

export function serializeVote(value: Vote, context?: TransitionInputContext): string {
  BaseContract.assertObject(value, context);
  const _raw = (value as unknown as { readonly [k: symbol]: unknown })[BaseContract.RECORD_RAW];
  if (typeof _raw === "string") return _raw;
  const fields: string[] = [];
  fields.push("owner: " + BaseContract.serializeAddress(value.owner, BaseContract.childInputContext(context, "owner")));
  fields.push("strategy: " + BaseContract.serializeIdentifier(value.strategy, BaseContract.childInputContext(context, "strategy")));
  fields.push("_nonce: " + BaseContract.serializeGroup(value._nonce, BaseContract.childInputContext(context, "_nonce")));
  return "{ " + fields.join(", ") + " }";
}

export function deserializeVote(value: string): Vote {
  const _fields = BaseContract.parseStruct(value);
  const _record: Vote = {
    owner: BaseContract.parseAddress(_fields["owner"]!),
    strategy: BaseContract.parseIdentifier(_fields["strategy"]!),
    _nonce: BaseContract.parseGroup(_fields["_nonce"] ?? ""),
  };
  Object.defineProperty(_record, BaseContract.RECORD_RAW, { value, enumerable: false });
  return _record;
}

export async function decryptVote(ciphertext: string, key: RecordDecryptionKey): Promise<Vote> {
  return BaseContract.decryptRecord(ciphertext, key, deserializeVote);
}

// ---------------------------------------------------------------------------
// Governance contract wrapper
// ---------------------------------------------------------------------------

export class Governance extends BaseContract {
  constructor() {
    super("governance.aleo");
  }

  readonly route = {
    locally: async (args: { readonly strategy: IdentifierInput; readonly maybe_strategy: IdentifierInput | null; readonly strategies: ReadonlyArray<IdentifierInput>; readonly vote: Vote }, options?: LocalExecutionOptions): Promise<LeoIdentifier> => {
      const _args: string[] = [
        BaseContract.serializeIdentifier(args.strategy, this.inputContext("route", "strategy")),
        (args.maybe_strategy != null ? "{ is_some: true, val: " + BaseContract.serializeIdentifier(args.maybe_strategy, this.inputContext("route", "maybe_strategy")) + " }" : "{ is_some: false, val: 'lionden_zero' }"),
        BaseContract.serializeArray(args.strategies, this.inputContext("route", "strategies"), (e: unknown, context: TransitionInputContext | undefined) => BaseContract.serializeIdentifier(e, context)),
        serializeVote(args.vote as Vote, this.inputContext("route", "vote")),
      ];
      const _result = await this.executeLocal("route", _args, options ?? {});
      return BaseContract.parseIdentifier(this.outputAt(_result, "route", 0));
    },

    failsLocally: async (args: { readonly strategy: IdentifierInput; readonly maybe_strategy: IdentifierInput | null; readonly strategies: ReadonlyArray<IdentifierInput>; readonly vote: Vote }, options?: LocalExecutionOptions): Promise<void> => {
      const _args: string[] = [
        BaseContract.serializeIdentifier(args.strategy, this.inputContext("route", "strategy")),
        (args.maybe_strategy != null ? "{ is_some: true, val: " + BaseContract.serializeIdentifier(args.maybe_strategy, this.inputContext("route", "maybe_strategy")) + " }" : "{ is_some: false, val: 'lionden_zero' }"),
        BaseContract.serializeArray(args.strategies, this.inputContext("route", "strategies"), (e: unknown, context: TransitionInputContext | undefined) => BaseContract.serializeIdentifier(e, context)),
        serializeVote(args.vote as Vote, this.inputContext("route", "vote")),
      ];
      await this.expectLocalFailure("route", _args, options ?? {});
    },

    captureLocalFailure: async (args: { readonly strategy: IdentifierInput; readonly maybe_strategy: IdentifierInput | null; readonly strategies: ReadonlyArray<IdentifierInput>; readonly vote: Vote }, options?: LocalExecutionOptions): Promise<LocalTransitionError> => {
      const _args: string[] = [
        BaseContract.serializeIdentifier(args.strategy, this.inputContext("route", "strategy")),
        (args.maybe_strategy != null ? "{ is_some: true, val: " + BaseContract.serializeIdentifier(args.maybe_strategy, this.inputContext("route", "maybe_strategy")) + " }" : "{ is_some: false, val: 'lionden_zero' }"),
        BaseContract.serializeArray(args.strategies, this.inputContext("route", "strategies"), (e: unknown, context: TransitionInputContext | undefined) => BaseContract.serializeIdentifier(e, context)),
        serializeVote(args.vote as Vote, this.inputContext("route", "vote")),
      ];
      return this.expectLocalFailure("route", _args, options ?? {});
    },

    submitted: async (args: { readonly strategy: IdentifierInput; readonly maybe_strategy: IdentifierInput | null; readonly strategies: ReadonlyArray<IdentifierInput>; readonly vote: Vote }, options?: OnChainExecutionOptions): Promise<SubmittedTransition> => {
      const _args: string[] = [
        BaseContract.serializeIdentifier(args.strategy, this.inputContext("route", "strategy")),
        (args.maybe_strategy != null ? "{ is_some: true, val: " + BaseContract.serializeIdentifier(args.maybe_strategy, this.inputContext("route", "maybe_strategy")) + " }" : "{ is_some: false, val: 'lionden_zero' }"),
        BaseContract.serializeArray(args.strategies, this.inputContext("route", "strategies"), (e: unknown, context: TransitionInputContext | undefined) => BaseContract.serializeIdentifier(e, context)),
        serializeVote(args.vote as Vote, this.inputContext("route", "vote")),
      ];
      return this.submitTransition("route", _args, options ?? {});
    },

    settled: async (args: { readonly strategy: IdentifierInput; readonly maybe_strategy: IdentifierInput | null; readonly strategies: ReadonlyArray<IdentifierInput>; readonly vote: Vote }, options?: OnChainExecutionOptions): Promise<SettledTransition> => {
      const _args: string[] = [
        BaseContract.serializeIdentifier(args.strategy, this.inputContext("route", "strategy")),
        (args.maybe_strategy != null ? "{ is_some: true, val: " + BaseContract.serializeIdentifier(args.maybe_strategy, this.inputContext("route", "maybe_strategy")) + " }" : "{ is_some: false, val: 'lionden_zero' }"),
        BaseContract.serializeArray(args.strategies, this.inputContext("route", "strategies"), (e: unknown, context: TransitionInputContext | undefined) => BaseContract.serializeIdentifier(e, context)),
        serializeVote(args.vote as Vote, this.inputContext("route", "vote")),
      ];
      return this.settleTransition("route", _args, options ?? {});
    },

    accepted: async (args: { readonly strategy: IdentifierInput; readonly maybe_strategy: IdentifierInput | null; readonly strategies: ReadonlyArray<IdentifierInput>; readonly vote: Vote }, options?: OnChainExecutionOptions): Promise<SettledTransition> => {
      const _args: string[] = [
        BaseContract.serializeIdentifier(args.strategy, this.inputContext("route", "strategy")),
        (args.maybe_strategy != null ? "{ is_some: true, val: " + BaseContract.serializeIdentifier(args.maybe_strategy, this.inputContext("route", "maybe_strategy")) + " }" : "{ is_some: false, val: 'lionden_zero' }"),
        BaseContract.serializeArray(args.strategies, this.inputContext("route", "strategies"), (e: unknown, context: TransitionInputContext | undefined) => BaseContract.serializeIdentifier(e, context)),
        serializeVote(args.vote as Vote, this.inputContext("route", "vote")),
      ];
      return this.expectAccepted("route", _args, options ?? {});
    },

    rejected: async (args: { readonly strategy: IdentifierInput; readonly maybe_strategy: IdentifierInput | null; readonly strategies: ReadonlyArray<IdentifierInput>; readonly vote: Vote }, options?: OnChainExecutionOptions): Promise<SettledTransition> => {
      const _args: string[] = [
        BaseContract.serializeIdentifier(args.strategy, this.inputContext("route", "strategy")),
        (args.maybe_strategy != null ? "{ is_some: true, val: " + BaseContract.serializeIdentifier(args.maybe_strategy, this.inputContext("route", "maybe_strategy")) + " }" : "{ is_some: false, val: 'lionden_zero' }"),
        BaseContract.serializeArray(args.strategies, this.inputContext("route", "strategies"), (e: unknown, context: TransitionInputContext | undefined) => BaseContract.serializeIdentifier(e, context)),
        serializeVote(args.vote as Vote, this.inputContext("route", "vote")),
      ];
      return this.expectRejected("route", _args, options ?? {});
    },
  } as const;

  async getSelected_strategy(key: IdentifierInput): Promise<LeoIdentifier | null> {
    const _result = await this.queryMapping("selected_strategy", BaseContract.serializeIdentifier(key, { programId: this.programId, input: "selected_strategy key" }));
    if (_result === null) return null;
    return BaseContract.parseIdentifier(_result);
  }
}

export function createGovernance(): Governance {
  return new Governance();
}
