// Auto-generated by @lionden/leo-compiler — do not edit
// Program: test.aleo

import { BaseContract, type LocalCallOptions, type BroadcastOptions, type TransitionCallResult } from "./BaseContract.js";

export interface A_Thing {
  readonly x: number;
}

export interface B_Thing {
  readonly y: bigint;
  readonly z: boolean;
}

// ---------------------------------------------------------------------------
// Serialization / deserialization helpers
// ---------------------------------------------------------------------------

export function serializeA_Thing(value: A_Thing): string {
  const fields: string[] = [];
  fields.push("x: " + value.x.toString() + "u32");
  return "{ " + fields.join(", ") + " }";
}

export function deserializeA_Thing(value: string): A_Thing {
  const _fields = BaseContract.parseStruct(value);
  return {
    x: BaseContract.parseNumber(_fields["x"]!),
  };
}

export function serializeB_Thing(value: B_Thing): string {
  const fields: string[] = [];
  fields.push("y: " + value.y.toString() + "u64");
  fields.push("z: " + String(value.z));
  return "{ " + fields.join(", ") + " }";
}

export function deserializeB_Thing(value: string): B_Thing {
  const _fields = BaseContract.parseStruct(value);
  return {
    y: BaseContract.parseBigInt(_fields["y"]!),
    z: BaseContract.parseBoolean(_fields["z"]!),
  };
}

// ---------------------------------------------------------------------------
// Test contract wrapper
// ---------------------------------------------------------------------------

export class Test extends BaseContract {
  constructor() {
    super("test.aleo");
  }

  async get_a(
    options?: LocalCallOptions,
  ): Promise<A_Thing> {
    const _args: string[] = [
    ];
    const _result = await this.executeLocal("get_a", _args, options ?? {});
    return deserializeA_Thing(_result.outputs[0]!);
  }

  async get_aBroadcast(
    options?: BroadcastOptions,
  ): Promise<TransitionCallResult> {
    const _args: string[] = [
    ];
    return this.broadcast("get_a", _args, options ?? {});
  }

  async get_b(
    options?: LocalCallOptions,
  ): Promise<B_Thing> {
    const _args: string[] = [
    ];
    const _result = await this.executeLocal("get_b", _args, options ?? {});
    return deserializeB_Thing(_result.outputs[0]!);
  }

  async get_bBroadcast(
    options?: BroadcastOptions,
  ): Promise<TransitionCallResult> {
    const _args: string[] = [
    ];
    return this.broadcast("get_b", _args, options ?? {});
  }
}

export function createTest(): Test {
  return new Test();
}
