// Auto-generated by @lionden/leo-compiler — do not edit
// Program: vault.aleo

import { BaseContract, type LocalCallOptions, type BroadcastOptions, type TransitionCallResult } from "./BaseContract.js";

export interface Policy {
  readonly max_amount: bigint;
  readonly admin: string;
}

export interface VaultStorage {
  readonly admin: string;
  readonly whitelist: string[];
  readonly policy: Policy;
  readonly policies: Policy[];
}

// ---------------------------------------------------------------------------
// Serialization / deserialization helpers
// ---------------------------------------------------------------------------

export function serializePolicy(value: Policy): string {
  const fields: string[] = [];
  fields.push("max_amount: " + value.max_amount.toString() + "u128");
  fields.push("admin: " + value.admin);
  return "{ " + fields.join(", ") + " }";
}

export function deserializePolicy(value: string): Policy {
  const _fields = BaseContract.parseStruct(value);
  return {
    max_amount: BaseContract.parseBigInt(_fields["max_amount"]!),
    admin: BaseContract.parseString(_fields["admin"]!),
  };
}

// ---------------------------------------------------------------------------
// Vault contract wrapper
// ---------------------------------------------------------------------------

export class Vault extends BaseContract {
  constructor() {
    super("vault.aleo");
  }

  async set_admin(
    new_admin: string,
    options?: LocalCallOptions,
  ): Promise<void> {
    const _args: string[] = [
      new_admin,
    ];
    const _result = await this.executeLocal("set_admin", _args, options ?? {});
  }
  
  async set_adminBroadcast(
    new_admin: string,
    options?: BroadcastOptions,
  ): Promise<TransitionCallResult> {
    const _args: string[] = [
      new_admin,
    ];
    return this.broadcast("set_admin", _args, options ?? {});
  }
}

export function createVault(): Vault {
  return new Vault();
}
