---
---

Intentionally empty: bootstraps the CI/CD + release pipeline (workflows, Dependabot, zizmor,
pinact, changesets, repo-settings docs) and normalizes release metadata (package `repository`
fields, internal dependency ranges). No published package behavior changed, so this releases
nothing.
